apiVersion: v1
kind: Service
metadata:
  name: {{ include "Project3-charts.fullname" . }}
  labels:
    {{- include "Project3-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Project3-charts.selectorLabels" . | nindent 4 }}
