apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Project3-charts.fullname" . }}-test-connection"
  labels:
    {{- include "Project3-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Project3-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
