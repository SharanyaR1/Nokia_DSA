apiVersion: v1
kind: Service
metadata:
  name: {{ include "nokiaweek5.fullname" . }}
  labels:
    {{- include "nokiaweek5.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nokiaweek5.selectorLabels" . | nindent 4 }}
