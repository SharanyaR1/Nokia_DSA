apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "salmon.fullname" . }}-test-connection"
  labels:
    {{- include "salmon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "salmon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
