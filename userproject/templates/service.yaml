apiVersion: v1
kind: Service
metadata:
  name: {{ include "userproject.fullname" . }}
  labels:
    {{- include "userproject.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "userproject.selectorLabels" . | nindent 4 }}
