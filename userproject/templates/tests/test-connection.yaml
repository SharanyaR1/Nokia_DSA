apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "userproject.fullname" . }}-test-connection"
  labels:
    {{- include "userproject.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "userproject.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
