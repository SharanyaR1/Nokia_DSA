apiVersion: v1
kind: Service
metadata:
  name: {{ include "week4.fullname" . }}
  labels:
    {{- include "week4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "week4.selectorLabels" . | nindent 4 }}
